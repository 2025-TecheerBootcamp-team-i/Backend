server:
  log_level: info

metrics:
  global:
    scrape_interval: 60s
  wal_directory: /tmp/agent/wal
  configs:
    - name: default
      remote_write:
        - url: https://prometheus-prod-49-prod-ap-northeast-0.grafana.net/api/prom/push
          basic_auth:
            username: 2926985
            password: ${GRAFANA_CLOUD_TOKEN}
          
          # 전송 직전 이름 강제 통일 (유지)
          write_relabel_configs:
            - target_label: instance
              replacement: 'backend-server'
              action: replace

      scrape_configs:
        - job_name: agent
          static_configs:
            - targets: ['localhost:12345']

integrations:
  node_exporter:
    enabled: true
    instance: 'backend-server'  # 👈 [복구] 이게 있어야 수집 목록에 뜹니다!
    rootfs_path: /host/root
    sysfs_path: /host/sys
    procfs_path: /host/proc

  cadvisor:
    enabled: true
    instance: 'backend-server'  # 👈 [복구] 얘도 다시 추가!
    docker_only: true